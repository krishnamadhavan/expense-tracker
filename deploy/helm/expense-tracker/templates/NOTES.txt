Expense Tracker has been installed as release "{{ .Release.Name }}".

1. Database migrations run as an initContainer (migrate.enabled={{ .Values.migrate.enabled }}).
2. Bootstrap admin (if no users yet): email={{ .Values.config.bootstrapEmail }}
   Password is in Secret {{ include "expense-tracker.secretName" . }} key bootstrap-password
   (default values use "changeme" — change it).

{{- if and .Values.ingress.enabled (not .Values.openshift.enabled) }}

Ingress host(s):
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/
{{- end }}
On Minikube, add to /etc/hosts: $(minikube ip) expense.local
  minikube addons enable ingress
{{- end }}

{{- if and .Values.openshift.enabled .Values.route.enabled }}

OpenShift Route: oc get route {{ include "expense-tracker.fullname" . }} -n {{ .Release.Namespace }}
{{- end }}

Port-forward (any cluster):
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "expense-tracker.fullname" . }} 8080:{{ .Values.service.port }}
  open http://127.0.0.1:8080

Health:
  /healthz  /readyz
